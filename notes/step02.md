# Step 2 - Arrays

## What I learned

- Access by index is fast because the memory address of each element can be calculated directly.
- Inserting at the beginning is slow because all existing elements need to be shifted one position to the right.
- append() is usually fast because the new element is added to the end of the array without moving existing elements, and resizing happens only occasionally.